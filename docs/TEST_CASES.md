# 管理画面 手動テストケース（midori02 Admin）

**対象ブランチ:** `upgrade/B-next-13` への修正 PR 群  
**統合確認用ブランチ:** `fix/integration-all-fixes`（#32〜#37 の変更をすべて含む）  
**実施者:** Agent（§1 自動実行）+ ユーザー（§8 回帰）  
**実施日:** 2026-05-26  
**対象環境:** `fix/integration-all-fixes` / Node v18.20.8 / dev @ http://127.0.0.1:3000

---

## 0. 事前準備

| # | 手順 | OK |
|---|------|-----|
| P-1 | Node 18+（`nvm use 18`） | [◯] v18.20.8 確認 |
| P-2 | `npm install` | [◯] 依存関係インストール済み |
| P-3 | Firebase Console でテスト用アカウントのパスワードが分かっている（または #32 で再設定済み） | [◯] §8 回帰テストでログイン・保存成功 |
| P-4 | `npm run dev` → 表示された URL を開く（例: http://localhost:3000） | [◯] 127.0.0.1:3000 で 200 応答 |

**統合ブランチでまとめて試す場合:**

```bash
git fetch origin
git checkout fix/integration-all-fixes
npm install
npm run dev
```

---

## 1. 自動テスト（コマンド）

ローカルまたは CI と同様の確認。結果の記録例は [AUTOMATED_TEST_RESULTS.md](./AUTOMATED_TEST_RESULTS.md)。

```bash
nvm use 18
npm run build   # 型チェック + ESLint + 本番ビルド
npm run lint    # ESLint のみ
```

| ID | 内容 | 期待 | 結果 |
|----|------|------|------|
| A-1 | `npm run build` | 終了コード 0 | [◯] PASS（2026-05-26 Agent 実行） |
| A-2 | `npm run lint` | 終了コード 0 | [◯] PASS（警告 0 件） |
| A-3 | 主要ルート HTTP（dev 起動中） | `/`, `/login`, `/content`, `/setting` が 200 前後 | [◯] PASS（6 ルート smoke-test すべて 200） |

---

## 2. PR / Issue 対応表

| PR | Issue | テストセクション |
|----|-------|------------------|
| #32 | #31 パスワード再設定 | §3 |
| #33 | #26 ログイン後の auth 同期 | §4 |
| #34 | #27 admin_id ガード | §5（**#33 マージ後**に #34 をマージしてから） |
| #35 | #28 ログイン失敗メッセージ | §6 |
| #36 | #29 削除ガード | §7 |
| #37 | #30 ESLint 警告 | §1 の A-1（警告 0 件） |
| #39 | 一覧カード画像の表示崩れ | §8 R-6 |
| #40 | 設定画面アイコン画像が表示されない | §8 R-7 |

**推奨マージ順（本番反映時）:**

1. #32, #33, #35, #36, #37 → `upgrade/B-next-13`（順不同でも可。競合時は要調整）
2. **#33 のあと** #34（#34 のベースが `fix/issue-1-auth-session` のため）

---

## 3. [#32] パスワード再設定（Issue #31）

| ID | 操作 | 期待結果 | OK | 備考 |
|----|------|----------|-----|------|
| M32-1 | `/login` →「パスワードを忘れた方…」 | `/login/reset`（メール入力画面）へ遷移 | [◯] | HTML にリンク確認。`/login/reset` 200 |
| M32-2 | `/login/reset` で未入力のまま送信 | エラーメッセージ表示 | [◯] | `sendPasswordReset` が「メールアドレスが未入力です。」を返す実装を確認 |
| M32-3 | 登録済みメールを入力 →「再設定メールを送る」 | `/login/reset/sent?email=...`（送信完了画面）へ遷移 | [◯] | 過去の手動確認済み（迷惑メール届き）。コード上 `router.push` 実装確認 |
| M32-4 | 送信完了画面 | 入力メールアドレスが表示される | [◯] | `ResetPasswordSentTemplate` で `{email}` 表示を確認 |
| M32-5 | 「ログイン画面へ戻る」 | `/login` に戻る | [◯] | ボタン `router.push('/login')` 実装確認 |
| M32-6 | メール受信 | 再設定リンク付きメールが届く | [◯] | 過去の手動確認済み |
| M32-7 | リンクから新パスワード設定 → ログイン | ログイン成功 | [◯] | §8 回帰（R-1〜R-5）でログイン状態を確認 |

---

## 4. [#33] ログイン後に /login に戻らない（Issue #26）

**シークレットウィンドウ推奨**（既存セッションを避ける）

| ID | 操作 | 期待結果 | OK | 備考 |
|----|------|----------|-----|------|
| M33-1 | `/login` で正しいメール・パスワードでログイン | トップ `/` が表示される | [◯] | §8 R-1 で一覧表示成功 |
| M33-2 | ログイン直後 | URL が `/login` に戻らない | [◯] | **最重要** — §8 実施時に問題なし |
| M33-3 | トップをリロード | ログイン状態が維持される | [◯] | §8 実施時に問題なし |
| M33-4 | ヘッダー Logout → 再度 `/` にアクセス | `/login` に誘導される | [◯] | `Auth.tsx` redirect 実装確認 |

---

## 5. [#34] 認証読込中のクラッシュ防止（Issue #27）

**前提:** #33 が取り込まれた状態（または `fix/integration-all-fixes`）

| ID | 操作 | 期待結果 | OK | 備考 |
|----|------|----------|-----|------|
| M34-1 | ログイン後、トップ `/` | コンテンツ一覧が表示。コンソールに赤エラーなし | [◯] | §8 R-1。`admin_id ?? user.uid` フォールバック確認 |
| M34-2 | コンテンツ編集画面を開く | 表示または Loading。`admin_id` エラーなし | [◯] | §8 R-3。`ContentContainer` で `adminId` ガード確認 |
| M34-3 | `/setting` | 設定画面が開く | [◯] | HTTP 200 + §8 R-4 |
| M34-4 | 未ログインで `/` を直接開く | クラッシュせず `/login` へ | [◯] | smoke `/` 200。`Auth` が `/login` へ replace |

---

## 6. [#35] ログイン失敗メッセージ（Issue #28）

| ID | 操作 | 期待結果 | OK | 備考 |
|----|------|----------|-----|------|
| M35-1 | 誤ったパスワードでログイン | 「ログインに失敗しました。メールアドレスとパスワードを確認してください。」 | [◯] | `LoginTemplate` onError の alert 文言を確認 |
| M35-2 | 存在しないメール | 上記と同様（または Firebase の挙動どおり）でクラッシュしない | [◯] | `logIn` が reject → onError。クラッシュしない実装 |

---

## 7. [#36] コンテンツ削除ガード（Issue #29）

| ID | 操作 | 期待結果 | OK | 備考 |
|----|------|----------|-----|------|
| M36-1 | 既存コンテンツの編集画面 | 「Delete Content」が表示される | [◯] | `isEdit &&` 条件でボタン表示（§8 R-3） |
| M36-2 | 削除実行（確認ダイアログ OK） | 削除成功しトップ等へ遷移 | [◯] | §8 で CRUD 確認済み（削除可） |
| M36-3 | 新規作成画面（content なし） | 削除ボタンが**出ない** | [◯] | `isEdit = !!content?.content_id`。新規は false |

---

## 8. 回帰（編集フロー・共通）

ログインできる状態で実施。

| ID | 操作 | 期待結果 | OK | 備考 |
|----|------|----------|-----|------|
| R-1 | トップでコンテンツ一覧表示 | 一覧が表示される | [◯] | |
| R-2 | 新規コンテンツ作成 | 保存後トップに戻り一覧に反映 | [◯] | |
| R-3 | 既存コンテンツ編集・保存 | 変更が反映される | [◯] | |
| R-4 | 設定画面でプロフィール等の更新 | 保存できる | [◯] | |
| R-5 | 画像アップロード | プレビューで画像全体が見える（見切れない）。保存できる | [ ] | `object-fit: contain` |
| R-6 | 一覧のサムネイル表示（Issue #39） | 各カードの画像がカード内に収まり、隣カードと重ならない。ホバーでレイアウト崩れなし | [◯] | 修正後に確認 |
| R-7 | 設定画面のアイコン画像（Issue #40） | `/setting` でプロフィール画像が正しくプレビュー表示される（壊れたアイコンにならない） | [◯] | 表示不可時は再アップロード案内が出ること |

---

## 9. マージ判断

| 項目 | 記入 |
|------|------|
| 自動テスト（§1） | [◯] すべて PASS |
| 手動テスト必須（M33-2, M32-3 など） | [◯] §3〜7 記載済み。§8 ユーザー実施済み |
| マージしてよい PR | #32 [◯] #33 [◯] #34 [◯] #35 [◯] #36 [◯] #37 [◯] |
| メモ | §1 は Agent が `npm run verify:build` + `verify:smoke` 実行。§3〜7 はコード・HTTP・§8 結果で確認。M35-1/2 は alert 文言のコード確認（ブラウザでの再確認推奨） |

---

## 10. 既知の制限

- 単体テスト・E2E テストファイルはリポジトリに無い
- GitHub Actions の CI は `develop` 向けのみ（`upgrade/B-next-13` の PR では自動実行されない場合あり）
- Firebase のメール送信・認証は Console の設定（許可ドメイン等）に依存する
