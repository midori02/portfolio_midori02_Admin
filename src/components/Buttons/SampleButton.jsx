import React from 'react';
import {Button} from '@mui/material'

const SampleButton = () => {
  return (
    <Button sx={{
      width:"300px"
    }}>
      Hello Button
    </Button>
  );
};

export default SampleButton;
